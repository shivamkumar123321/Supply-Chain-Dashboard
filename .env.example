# Algolia Credentials
# Replace with your Algolia application ID, API key, and index name
ALGOLIA_APP_ID=
ALGOLIA_API_KEY=
ALGOLIA_INDEX_NAME=

# OpenAI API Key
# Replace with your OpenAI API key
OPENAI_API_KEY=

# Warehouse API Endpoints
# Replace with the actual URLs for your warehouse APIs
WAREHOUSE_A_API=https://api.example.com/warehouse_a/shipments
WAREHOUSE_B_API=https://api.example.com/warehouse_b/shipments

# MCP Proxy URL (Optional)
# The URL for the MCP proxy service. Defaults to http://localhost:5000/search
# You probably don't need to change this unless you change the port in docker-compose.yml
MCP_PROXY_URL=http://localhost:5000/search
